using System.ComponentModel.DataAnnotations;

namespace PartsPasal.Domain.Entities;

/// <summary>
/// Represents a vendor from whom vehicle parts are purchased.
/// Required Feature: Admin can manage vendor details (CRUD).
/// </summary>
public class Vendor
{
    // Id, Name, ContactPerson, Phone, Email, Address, Category (Parts/Services)
}
